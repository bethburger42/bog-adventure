<h1>Create a New Creature</h1>
<div class="well">
	<%= bootstrap_form_for @creature do |creature| %>
	<%= creature.text_field :name %>
	<%= creature.text_area :description %> 

	<%= creature.primary %>
 </div>
<% end %>